[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "ferran-agentbridge"
version = "2.0.0"
description = "Model-first Codex execution with observable local account routing"
readme = "README.md"
requires-python = ">=3.11"
authors = [{name = "Ferran Gutierrez"}]
dependencies = []

[project.optional-dependencies]
test = ["pytest>=8,<10"]

[project.scripts]
agentbridge = "agentbridge.cli:main"

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-data]
agentbridge = ["py.typed", "provider_contract_index.json"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
addopts = "-ra"
